/**
 * app/(app)/alert.tsx
 *
 * The Alert screen is shown right after login.
 * It's a standalone screen (no bottom tabs yet).
 * When a dispatch is accepted, the user navigates to the dispatch tabs.
 */
import AlertScreen from '../../src/screens/AlertScreen';
export default AlertScreen;